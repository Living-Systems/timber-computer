---

import { getAllPagesWithSlugs } from '../../lib/api';
// Component Imports
import BaseLayout from '../../layouts/BaseLayout.astro';

// Component Script:
// You can write any JavaScript/TypeScript that you'd like here.
// It will run during the build, but never in the browser.
// All variables are available to use in the HTML template below.
import LearnNav from '../../components/Learn/LearnNav.vue'
import Header from '../../components/UI/Header.vue'

let title = 'Learn';
const getPages = await getAllPagesWithSlugs();
const pages    = getPages.data;
let bodyClass = 'bg-gold';
---



<BaseLayout title={ title } bodyClass={bodyClass}>

    <div slot="header">
        <Header link-to="/computer" link-name="Start to Compute" header-class="border-b"/>
    </div>

    <div slot="subnav">
        <LearnNav pages={ pages }/>
    </div>

    <div slot="main">
        <section class="flow-200 px-body">
            <h1>
                Let’s learn
            </h1>
        </section>
    </div>

</BaseLayout>
